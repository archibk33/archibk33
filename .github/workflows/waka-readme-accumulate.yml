name: Waka Readme - Accumulate v5

on:
  workflow_dispatch:
    inputs:
      summary_date:
        description: "Override date to fetch (YYYY-MM-DD). Leave empty for auto."
        required: false
      backfill_days:
        description: "Backfill last N days (0 = off)."
        required: false
        default: "0"
  schedule:
    - cron: "5 0 * * *"  # ежедневно 00:05 UTC

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # нужно для pull --rebase и корректного push

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Update README with WakaTime stats (accumulate)
        env:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          API_BASE_URL: https://api.wakatime.com/api/v1
          TIME_RANGE: today
          LANG_COUNT: "0"  # 0 = показать все языки
          IGNORED_LANGUAGES: ""
          TARGET_PATH: README.md
          STORE_PATH: .github/waka_store.json
          SUMMARY_DATE: ${{ inputs.summary_date }}
          BACKFILL_DAYS: ${{ inputs.backfill_days }}
        run: |
          echo "Starting WakaTime accumulation script..."
          python .github/workflows/scripts/update_waka_readme.py

      - name: Commit & push if changed (safe rebase)
        run: |
          git config user.name  "${{ github.actor }}"
          git config user.email "${{ github.actor }}@users.noreply.github.com"
          # Всегда убедимся, что локальная ветка синхронизирована перед коммитом
          BRANCH="${GITHUB_REF_NAME:-main}"
          git fetch origin "$BRANCH"
          git checkout "$BRANCH" || git checkout -B "$BRANCH"
          git pull --rebase origin "$BRANCH" || true

          if [[ -n "$(git status --porcelain)" ]]; then
            git add README.md .github/waka_store.json || true
            git commit -m "🔁 WakaReadme: accumulated metrics update" || true
            git push origin HEAD:"$BRANCH" --force-with-lease
          else
            echo "Nothing to commit."
          fi


